# 好友系統插件配置

# 好友配置
friend:
  max_friends: 50
  auto_accept_invite: false

# 黑名單配置
block:
  max_blocks: 100
  block_message: "§c你已被加入黑名單"

# 通知配置
notify:
  on_friend_add: true
  on_friend_remove: true
  on_block: true
